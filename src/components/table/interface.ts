/**
 * IMPORTS
 */

type WrapperTextFooterProps = {
  background?: boolean;
};

type TextNumberPageProps = {
  color?: string;
};

/**
 * EXPORTS
 */
export type { WrapperTextFooterProps, TextNumberPageProps };
